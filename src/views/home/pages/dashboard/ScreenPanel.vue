<script setup lang="ts">
defineProps<{
  title: string
}>()
</script>

<template>
  <article class="frame">
    <span class="frame__corner frame__corner--tl" />
    <span class="frame__corner frame__corner--tr" />
    <span class="frame__corner frame__corner--bl" />
    <span class="frame__corner frame__corner--br" />
    <header class="frame__head">
      <h3 class="frame__title">{{ title }}</h3>
      <slot name="extra" />
    </header>
    <div class="frame__body">
      <slot />
    </div>
  </article>
</template>

<style scoped>
.frame {
  position: relative;
  display: flex;
  flex-direction: column;
  min-height: 0;
  padding: 14px 14px 12px;
  background:
    linear-gradient(180deg, rgba(14, 36, 58, 0.72), rgba(8, 18, 34, 0.55));
  border: 1px solid rgba(56, 189, 248, 0.18);
  box-shadow: inset 0 0 40px rgba(14, 165, 233, 0.05);
  box-sizing: border-box;
}

.frame__corner {
  position: absolute;
  width: 14px;
  height: 14px;
  border-color: #38bdf8;
  border-style: solid;
  pointer-events: none;
}

.frame__corner--tl {
  top: -1px;
  left: -1px;
  border-width: 2px 0 0 2px;
}

.frame__corner--tr {
  top: -1px;
  right: -1px;
  border-width: 2px 2px 0 0;
}

.frame__corner--bl {
  bottom: -1px;
  left: -1px;
  border-width: 0 0 2px 2px;
}

.frame__corner--br {
  bottom: -1px;
  right: -1px;
  border-width: 0 2px 2px 0;
}

.frame__head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
  margin-bottom: 10px;
  padding-left: 10px;
  position: relative;
}

.frame__head::before {
  content: '';
  position: absolute;
  left: 0;
  top: 50%;
  width: 4px;
  height: 14px;
  transform: translateY(-50%);
  background: linear-gradient(180deg, #22d3ee, #0ea5e9);
  box-shadow: 0 0 10px rgba(34, 211, 238, 0.7);
}

.frame__title {
  margin: 0;
  font-size: 13px;
  font-weight: 650;
  letter-spacing: 0.08em;
  color: #e0f2fe;
}

.frame__body {
  flex: 1;
  min-height: 0;
  display: flex;
  flex-direction: column;
}

.frame__body > :deep(*) {
  flex: 1 1 auto;
  min-height: 0;
  height: 100%;
}
</style>
